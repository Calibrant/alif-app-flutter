name: flutter_app_alif
description: A new Flutter project.

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter_localizations:
    sdk: flutter
  flutter:
    sdk: flutter
  sqflite: ^1.3.0
  path_provider: ^2.0.1
  intl: ^0.17.0
  provider: ^4.3.3
  cupertino_icons: ^1.0.2
  adaptive_theme: ^2.2.0
  flutter_slidable: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - images/
 
flutter_intl:
  enabled: true

  
